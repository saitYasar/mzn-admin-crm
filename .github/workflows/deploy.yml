name: Secrets Check

on:
  workflow_dispatch:

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: 🔐 Secrets erişim kontrolü
        shell: bash
        run: |
          check_secret() {
            local name="$1"
            local value="${!name}"
            if [ -n "$value" ]; then
              local len=${#value}
              local hash=$(printf "%s" "$value" | sha256sum | cut -c1-8)
              echo "$name ✅ var | length: $len | hash8: $hash"
            else
              echo "$name ❌ yok"
            fi
          }

          # Buraya kontrol etmek istediğin secret isimlerini yaz
          check_secret "VERCEL_TOKEN"
          check_secret "VERCEL_PROJECT_ID"
          check_secret "VERCEL_ORG_ID"
          check_secret "INFISICAL_DOMAIN"
          check_secret "INFISICAL_IDENTITY_ID"
